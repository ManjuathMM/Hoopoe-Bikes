import React from 'react';
import AlertBanner from '../AlertBanner';
import './Topbar.css';

const Topbar = () => {
  return (
    <div className="topbar">
      <AlertBanner />
    </div>
  );
};

export default Topbar;
